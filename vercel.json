{
  "version": 2,
  "buildCommand": "npm install && npm run build:frontend",
  "outputDirectory": "Frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}